apiVersion: v1
kind: Secret
metadata:
  name: translation-svc-secrets
  namespace: translator
type: Opaque
stringData:
  # Replace with actual DeepL API key before deploying
  # For local development, create this secret manually:
  # kubectl create secret generic translation-svc-secrets \
  #   --namespace=translator \
  #   --from-literal=deepl-api-key=YOUR_DEEPL_API_KEY
  deepl-api-key: "REPLACE_WITH_YOUR_API_KEY"
