apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: translator

resources:
  - deployment.yaml
  - service.yaml
  # Note: secret.yaml is not included by default
  # Create the secret manually with your actual API key
